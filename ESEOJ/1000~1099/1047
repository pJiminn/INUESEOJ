// 문제 설명
// 정수 두 개(a, b) 를 입력받아 a를 b로 나눈 나머지를 출력해보자.
// (단, 0 <= a , b <=2147483647, b는 0이 아니다.)

// 입력
// 정수 두 개(a, b)가 공백을 두고 입력된다.
// (단, 0 <= a, b <= 2147483647, b는 0이 아니다.)

// 출력
// a 를 b로 나누고 난 후 남는 나머지를 출력한다.


#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a,b;

	scanf("%d %d", &a,&b);

	printf("%d", a%b);

	return 0;
}