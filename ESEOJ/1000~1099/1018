// 문제 설명
// 정수(int) 2개를 입력받고 그대로 출력해보자.

// 입력
// 두 개의 정수가 공백으로 구분되어 입력된다.

// 출력
// 입력받은 두 정수를 공백으로 구분하여 출력한다.

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
int a,b;
 scanf("%d %d",&a,&b);
 printf("%d %d",a,b);

	return 0;
}