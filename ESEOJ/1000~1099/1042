// 문제 설명
// 정수 2개를 입력받아 합을 출력해보자.
// (단, 입력되는 정수는 -1073741824 ~ 1073741824 이다.)

// 입력
// 두 개의 정수가 공백으로 구분되어 입력된다.
// ** 주의 : 계산의 결과가 int 범위일지 넘어서는지 잘 생각해 보아야 한다.

// 출력
// 두 정수의 합을 출력한다.


#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a,b;

	scanf("%d %d", &a,&b);

	printf("%d", a+b);

	return 0;
}