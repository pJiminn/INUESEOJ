// 문제 설명
// 두 개의 문자(ASCII CODE)를 입력받아서 순서를 바꿔 출력해보자.

// 입력
// 두 개의 문자가 공백으로 구분되어 입력된다.

// 출력
// 두 문자의 순서를 바꿔 출력한다.


#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
char a,b;
 scanf("%c %c",&a,&b);
 printf("%c %c",b,a);

	return 0;
}