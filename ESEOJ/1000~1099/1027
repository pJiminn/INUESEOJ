// 문제 설명
// 실수 하나를 입력받는다.
// 정수 부분과 실수 부분을 나누어 따로 출력한다.

// 입력
// 실수 하나가 입력된다.
// (단, 입력 값은 절댓값이 10000을 넘지 않으며, 소수점이하 자릿수는 최대 6자리까지이다.)

// 출력
// 첫 번째 줄에 정수 부분을
// 두 번째 줄에 실수 부분을 출력한다.


#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	
	int a, b;

	scanf("%d.%d", &a,&b);

	printf("%d\n%d", a,b);


	return 0;
}