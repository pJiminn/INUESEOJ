// 문제 설명
// 정수 두 개(a, b)를 입력받아 
// (0 <= a <= 10, 0 <= b <= 10)
// a를 2^b 배로 곱해 출력해보자.

// 입력
// 정수 두 개가 공백을 두고 입력된다.
// (0 <= a <= 10, 0 <= b <= 10)

// 출력
// a 를 2^b 배 만큼 곱해 출력한다


#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a, b;

	scanf("%d %d", &a, &b);

	printf("%d\n", a << b);


	return 0;
}